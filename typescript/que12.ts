// Create a union type for API status: "loading" | "success" | "error".

let status : "loading" | "success" | "Error"

status="success"

//Not assignable
// status="ABC"

// We can assign only upper 3 string to variable nothing Else